% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Molecule3D.R
\name{fetch_id_of_first_proper_rotation_axis_with_order}
\alias{fetch_id_of_first_proper_rotation_axis_with_order}
\title{Fetch the ID of the first proper rotation axis of order Cn}
\usage{
fetch_id_of_first_proper_rotation_axis_with_order(molecule, Cn)
}
\arguments{
\item{molecule}{A \code{\link{Molecule3D}} object containing one or more
symmetry elements.}

\item{Cn}{Integer (or integer-like numeric). The order of the proper rotation
axis to search for (e.g. \code{2}, \code{3}, \code{6}).}
}
\value{
A \strong{character scalar} giving the ID of the first matching proper rotation
axis, or \code{NA_character_} if none exist.
}
\description{
Returns the identifier (ID) of the \strong{first} stored
\code{\link{ProperRotationAxis}} within a \code{\link{Molecule3D}}
whose order (\verb{@n}) matches the user-supplied value \code{Cn}.

This is a convenience helper for quickly selecting a representative
symmetry axis of a given order (e.g. the “principal” C\eqn{_2}, C\eqn{_3},
C\eqn{_6}, etc.).
}
\details{
The function operates on the molecule's internal
\verb{@symmetry_elements@elements} list, which stores each symmetry element under
a unique ID (the list name). It:

\enumerate{
\item Iterates over all stored symmetry elements.
\item Filters only those that are
\code{\link{ProperRotationAxis}} objects.
\item Checks whether their order (\code{axis@n}) matches \code{Cn}.
\item Returns the ID (list name) of the \strong{first} match.
}

If the molecule contains no proper rotation axes with order \code{Cn}, the function
returns \code{NA_character_}.
}
\examples{
atoms <- data.frame(
  eleno = c(1,2),
  elena = c("C","O"),
  x = c(0,1), y = c(0,0), z = c(0,0)
)
m <- Molecule3D("CO", atoms = atoms, bonds = minimal_bonds())

m <- add_symmetry_element_to_molecule(
  m,
  ProperRotationAxis(n = 2L, posA = c(0,0,0), posB = c(0,0,1), label = "C2(z)")
)

fetch_id_of_first_proper_rotation_axis_with_order(m, Cn = 2)

}
\seealso{
\code{\link[=fetch_all_proper_rotation_axes_with_order]{fetch_all_proper_rotation_axes_with_order()}},
\code{\link[=fetch_symmetry_element_from_molecule]{fetch_symmetry_element_from_molecule()}},
\code{\link{ProperRotationAxis}}
}
