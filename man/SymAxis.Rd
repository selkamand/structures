% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SymAxis.R
\name{SymAxis}
\alias{SymAxis}
\title{SymAxis: Rotational Symmetry Axis (C_n)}
\usage{
SymAxis(Cn, posA, posB = NULL, direction = NULL, L = NULL, label = "unnamed")
}
\arguments{
\item{Cn}{Numeric (length 1). Axis fold/order (\eqn{\ge} 1, whole number).}

\item{posA}{Numeric (length 3). A point on the axis: \code{c(x,y,z)}.}

\item{posB}{Numeric (length 3). A different point on the axis; mutually exclusive
with \code{direction}. When supplied, \code{direction} and \code{L} are inferred.}

\item{direction}{Numeric (length 3). Orientation vector; normalized on set.
Mutually exclusive with \code{posB}. If used without \code{L}, \code{L} defaults to 1.}

\item{L}{Numeric (length 1). Positive length from \code{posA} to \code{posB}.
Defaults to \code{1} when \code{direction} is supplied and \code{L} is omitted.}

\item{label}{Character scalar. Optional user-defined name/description.}
}
\value{
An S7 object of class \code{SymAxis}.
}
\description{
The SymAxis object represents a proper rotation axis in molecular or solid-state symmetry.
The axis is defined by a point \code{posA}, an orientation \code{direction}
(unit vector). A secondary point on this defined axis (\code{posB}) can also be
derived as \code{posA + direction * L} and is read-only.
}
\details{
\itemize{
\item \code{Cn} is the order (fold) of the axis, i.e. rotation by \code{360/Cn} degrees.
\item Construction options:
\itemize{
\item Supply \code{posA} and \code{posB}: \code{direction} and \code{L} are inferred.
\item Supply \code{posA} and \code{direction}: \code{direction} is normalized; if
\code{L} is missing it defaults to \code{1}.
}
\item Setting \code{direction} normalizes it but does not change \code{L}; change \code{L}
to adjust the span from \code{posA} to \code{posB}.
}
}
\note{
\code{posB} is read-only and computed as \code{posA + direction * L}.
}
\examples{
# Define via two points
SymAxis(Cn = 3L, posA = c(0,0,0), posB = c(0,0,1))

# Define via direction; L defaults to 1
SymAxis(Cn = 2L, posA = c(0,0,0), direction = c(0,0,5))

# Direction + explicit L
SymAxis(Cn = 2L, posA = c(1,0,0), direction = c(0,0,1), L = 2)

}
