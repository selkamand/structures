% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Class.R
\name{fetch_atom_position}
\alias{fetch_atom_position}
\title{Fetch atom Cartesian coordinates}
\usage{
fetch_atom_position(x, eleno, careful = TRUE)
}
\arguments{
\item{x}{A \code{Molecule3D} object.}

\item{eleno}{Character or numeric vector of atom IDs (matching \code{x@atoms$eleno}).}

\item{careful}{Logical. If \code{TRUE} (default), validates \code{x} and that all
\code{eleno} exist; otherwise missing IDs yield \code{NA} coordinates.}
}
\value{
If a single \code{eleno} is supplied, a named numeric vector
\code{c(x, y, z)}. If multiple, a matrix with columns \code{x}, \code{y}, \code{z}
and row names equal to the requested \code{eleno}.
}
\description{
Returns the 3D position(s) of one or more atoms by \code{eleno}.
}
\examples{
# Read Data
path <- system.file(package = "structures", "benzene.mol2")
molecule <- read_mol2(path)

# Single atom position
# fetch_atom_position(molecule, "1")

# Multiple atoms (rows named by eleno)
# fetch_atom_position(molecule, c("1","2","3"))

}
\seealso{
\code{\link{compute_distance_between_atoms}}
}
