% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Molecule3D.R
\name{filter_atoms_by_element}
\alias{filter_atoms_by_element}
\title{Filter molecule for specific atoms by element}
\usage{
filter_atoms_by_element(x, element)
}
\arguments{
\item{x}{A \code{\link{Molecule3D}} object.}

\item{element}{Character vector of element symbols to keep (matching
\code{x@atoms$element}, e.g. \code{"C"}, \code{"H"}, \code{"O"}).}
}
\value{
A \code{\link{Molecule3D}} object containing only atoms whose
\code{element} is in \code{element}, and the bonds between them.
}
\description{
Filters a molecule to include only atoms whose \code{element} (chemical symbol)
matches one or more requested values. Orphaned bonds (i.e. bonds whose
endpoints are not both retained) are automatically dropped, in the same way
as \code{\link[=filter_atoms]{filter_atoms()}}.
}
\examples{
path <- system.file(package = "structures", "benzene.mol2")
molecule <- read_mol2(path)

# Keep only carbon atoms
molecule_C <- filter_atoms_by_element(molecule, element = "C")

}
