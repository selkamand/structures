% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1-SymmetryElements.R
\name{ProperRotationAxis}
\alias{ProperRotationAxis}
\title{ProperRotationAxis: rotational symmetry axis (C_n)}
\usage{
ProperRotationAxis(
  n,
  posA,
  posB = NULL,
  direction = NULL,
  L = NULL,
  label = "unnamed"
)
}
\arguments{
\item{n}{Numeric scalar. Axis fold/order (whole number >= 1).}

\item{posA}{Numeric length-3 vector giving a point on the axis (\code{c(x, y, z)}).}

\item{posB}{Optional numeric length-3 vector giving a second point on the axis.
Mutually exclusive with \code{direction}.}

\item{direction}{Optional numeric length-3 vector giving the axis direction.
Normalised on set. Mutually exclusive with \code{posB}.}

\item{L}{Numeric scalar. Positive distance between \code{posA} and \code{posB}. If
\code{direction} is supplied and \code{L} is omitted, \code{L} defaults to 1.}

\item{label}{Character scalar. Optional user-defined name or description
for the symmetry element.}
}
\value{
An S7 object of class \code{ProperRotationAxis}.
}
\description{
Represents a proper rotation axis (C\eqn{_n}) in 3D. The axis is defined by a
point \code{posA} and an orientation \code{direction} (unit vector). A second point
\code{posB} lies along the axis at distance \code{L} from \code{posA} and is derived as
\code{posA + direction * L}.
}
\details{
\itemize{
\item \code{n} is the order (fold) of the axis, i.e. rotation by \code{360 / n} degrees.
\item You can construct an axis either by:
\itemize{
\item supplying \code{posA} and \code{posB} (direction and \code{L} are inferred), or
\item supplying \code{posA} and \code{direction} (direction is normalised; \code{L} defaults to 1
if not given).
}
\item \code{posB} is read-only and always computed from \code{posA}, \code{direction}, and \code{L}.
}
}
\examples{
# Define via two points (C3 axis along +Z through the origin)
ax1 <- ProperRotationAxis(
  n   = 3L,
  posA = c(0, 0, 0),
  posB = c(0, 0, 1),
  label = "C3(z)"
)

# Define via direction (C2 axis along +Z; L defaults to 1)
ax2 <- ProperRotationAxis(
  n = 2L,
  posA = c(0, 0, 0),
  direction = c(0, 0, 5),
  label = "C2(z)"
)

ax1@type   # "Proper Rotation Axis"

}
