% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Class.R
\name{fetch_all_symmetry_axes_with_order}
\alias{fetch_all_symmetry_axes_with_order}
\title{Fetch all symmetry axes of a given order (Cn)}
\usage{
fetch_all_symmetry_axes_with_order(molecule, Cn)
}
\arguments{
\item{molecule}{A \code{\link{Molecule3D}} object.}

\item{Cn}{Integer (or integer-like numeric) fold/order to match (e.g., \code{2}, \code{3}, \code{6}).}
}
\value{
\itemize{
\item \code{NULL} if the molecule contains no symmetry axes.
\item Otherwise, a \emph{list} of matching \code{\link{SymAxis}} objects.
The list may be empty if no axes have the requested \code{Cn}.
}
}
\description{
Returns all \code{\link{SymAxis}} objects stored in a
\code{\link{Molecule3D}} whose order \code{Cn} matches the requested value.
}
\examples{
atoms <- data.frame(
  eleno = c(1, 2),
  elena = c("C","O"),
  x = c(0, 1), y = c(0, 0), z = c(0, 0)
)
bonds <- data.frame(bond_id = 1, origin_atom_id = 1, target_atom_id = 2)
m <- Molecule3D("CO", atoms = atoms, bonds = bonds)

# Append a few axes
m <- add_symmetry_axis(m, SymAxis(Cn = 2, posA = c(0,0,0), posB = c(0,0,1)))
m <- add_symmetry_axis(m, SymAxis(Cn = 3, posA = c(1,0,0), posB = c(1,0,1)))

# Fetch by order
fetch_all_symmetry_axes_with_order(m, 2)   # list of C2 axes
fetch_all_symmetry_axes_with_order(m, 5)   # NULL or empty list if none

}
\seealso{
\link{SymAxis}, \code{\link[=add_symmetry_axis]{add_symmetry_axis()}}, \code{\link[=is_symmetry_axis]{is_symmetry_axis()}},
\link{Molecule3D}
}
