% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1-SymmetryElements.R
\name{combine_symmetry_element_collections}
\alias{combine_symmetry_element_collections}
\title{Combine two SymmetryElementCollections}
\usage{
combine_symmetry_element_collections(collection1, collection2)
}
\arguments{
\item{collection1}{A \code{\link{SymmetryElementCollection}} object
providing the base collection.}

\item{collection2}{A \code{\link{SymmetryElementCollection}} object whose
elements will be appended to \code{collection1}.}
}
\value{
A \code{\link{SymmetryElementCollection}} containing all elements
from both inputs, with updated \code{ids}.
}
\description{
Merges the symmetry elements and IDs from two
\code{\link{SymmetryElementCollection}} objects into a single collection. The IDs
from \code{collection2} are offset so that all IDs in the combined collection
remain unique.
}
\examples{
mp <- MirrorPlane(normal = c(0,0,1), position = c(0,0,0), label = "σ_xy")
ci <- CentreOfInversion(position = c(0,0,0), label = "i")

coll1 <- SymmetryElementCollection(elements = list(mp), ids = 1)
coll2 <- SymmetryElementCollection(elements = list(ci), ids = 1)

coll12 <- combine_symmetry_element_collections(coll1, coll2)
coll12@ids   # e.g. c(1, 2)

}
