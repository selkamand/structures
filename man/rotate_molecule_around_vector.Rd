% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Molecule3D.R
\name{rotate_molecule_around_vector}
\alias{rotate_molecule_around_vector}
\title{Rotate a Molecule3D around an arbitrary axis through a point}
\usage{
rotate_molecule_around_vector(molecule, axis, position = c(0, 0, 0), angle)
}
\arguments{
\item{molecule}{A \code{\link{Molecule3D}} object.}

\item{axis}{Numeric length-3 vector giving the axis direction (need not be unit).}

\item{position}{Numeric length-3 vector giving a point on the axis (the pivot line).
Defaults to \code{c(0, 0, 0)}.}

\item{angle}{Numeric scalar in radians (positive = right-hand rule about \code{axis}).}
}
\value{
A \code{Molecule3D} object with rotated coordinates, anchor, and symmetry axes.
}
\description{
Rotates all atom coordinates in a \code{\link{Molecule3D}} about a 3D axis
that passes through a specified point, by a given angle (radians). The
rotation is applied via \code{\link[=transform_molecule]{transform_molecule()}}, using
\code{move::rotate_vector_around_axis_through_point()} per-row. The molecule's
\verb{@anchor} and any stored symmetry axes are rotated consistently.
}
\details{
This is a convenience wrapper around \code{\link{transform_molecule}()} that
delegates the point-wise rotation to
\code{move::rotate_vector_around_axis_through_point(p, rotation_axis, point_on_axis, angle)}.
The axis direction is interpreted up to scale; only its orientation matters.
}
\examples{
# Rotate benzene 45° around the global z-axis through the origin
# m <- structures::read_mol2(system.file("benzene.mol2", package = "structures"))
# m_rot <- rotate_molecule_around_vector(m, axis = c(0,0,1), position = c(0,0,0), angle = pi/4)

}
\seealso{
\code{\link{transform_molecule}}, \code{move::rotate_vector_around_axis_through_point}
}
